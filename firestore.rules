rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents/tweets {
    match /{document=**} {
      allow read, write: if
          request.time < timestamp.date(2024, 12, 31);
    }
  }
}